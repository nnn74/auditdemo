# Demo of a problem  Audit.EntityFramework
Demonstration of a problem I have with `OwnsOne`.

Configured against Sqlite, but I have also tested against SQL Server.
It works when Audit.EntityFramework is removed.

